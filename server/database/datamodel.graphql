type Post {
  id: ID! @unique
  isPublished: Boolean! @default(value: false)
  title: String!
  text: String!
}

type Person {
  id: ID! @unique
  name: String! @unique
  address: String!
  age: Int!
  interests: String!
  picture: String!
}